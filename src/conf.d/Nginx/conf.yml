enabled: true
uniquekey:
  name: "host"
  DataType: IPv4
  ValueType: "RandomFixed"
  Value: "10.10.10.1"
  NumUniqKey: 5
period: 10s
#LogsPerSec: 600
MBPerDay: 100

# Add the timestamp field name and type. Type could be epoch_in_seconds, epoch_in_ms or datetime_rfc3339
timestamp:
  name: timestamp
  type: epoch_in_seconds

# Include following yml files from this directory.. To include all, please use ‘*’
#Include_sub_modules: [metric, logs]
Include_sub_modules: [logs]
#Include_sub_modules: [metrics]
